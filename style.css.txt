* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  user-select: none;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

body, html {
  width: 100%;
  height: 100%;
  overflow: hidden;
  background-color: #0f172a;
  color: #f8fafc;
}

/* --- LOBBY & ÉCRANS DE CONFIGURATION --- */
.lobby-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  padding: 20px;
  text-align: center;
  overflow-y: auto;
}

.lobby-container h2 { font-size: 1.8rem; margin-bottom: 4px; color: #38bdf8; }
.subtitle { font-size: 0.9rem; color: #94a3b8; margin-bottom: 20px; }

.lobby-box {
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 16px;
  padding: 20px;
  width: 100%;
  max-width: 360px;
  text-align: left;
}

.lobby-box h3 { font-size: 1rem; margin-bottom: 10px; color: #e2e8f0; }

.lobby-box input[type="text"], .select-box {
  width: 100%;
  padding: 10px;
  border-radius: 8px;
  border: 1px solid #334155;
  background: #0f172a;
  color: white;
  font-size: 1rem;
  margin-bottom: 12px;
}

.avatar-pick-section { display: flex; align-items: center; gap: 10px; margin-bottom: 12px; }
.avatar-btn { flex: 1; text-align: center; font-size: 0.85rem; padding: 8px; cursor: pointer; }
#avatar-preview-container img { width: 40px; height: 40px; border-radius: 50%; object-fit: cover; border: 2px solid #38bdf8; }
.no-avatar { font-size: 0.75rem; color: #94a3b8; }

.role-select { display: flex; gap: 10px; margin-bottom: 8px; }

.btn-role {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  border: 2px solid #334155;
  background: #0f172a;
  color: #94a3b8;
  font-weight: bold;
}
.btn-role.selected { border-color: #38bdf8; background: #0284c7; color: white; }

.info-text { font-size: 0.8rem; color: #38bdf8; margin-top: 8px; }
.text-center { text-align: center; }
.divider { border: 0; height: 1px; background: #334155; margin: 14px 0; }

.btn-primary, .btn-secondary, .action-btn {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: none;
  font-weight: bold;
  font-size: 1rem;
  cursor: pointer;
}

.btn-primary { background: #0284c7; color: white; }
.btn-secondary { background: #334155; color: white; width: auto; padding: 0 16px; }

.join-box { display: flex; gap: 8px; margin-top: 8px; }
.join-box input {
  flex: 1;
  padding: 10px;
  border-radius: 8px;
  border: 1px solid #334155;
  background: #0f172a;
  color: white;
  text-align: center;
  font-size: 1rem;
  margin: 0 !important;
}

.players-list { margin: 10px 0; font-size: 0.9rem; color: #cbd5e1; }
.player-row { display: flex; align-items: center; gap: 10px; padding: 6px 0; border-bottom: 1px solid #334155; }
.player-row img { width: 30px; height: 30px; border-radius: 50%; object-fit: cover; }

/* --- INTERFACE DE JEU & VIGNETTAGE LUMINEUX --- */
#app-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100vw;
  position: relative;
}

/* Effet halo fondu sur les bords */
#vignette-overlay {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  pointer-events: none;
  z-index: 999;
  transition: box-shadow 0.5s ease;
}

.status-ok #vignette-overlay { box-shadow: inset 0 0 40px rgba(34, 197, 94, 0.4); }
.status-warning #vignette-overlay { box-shadow: inset 0 0 50px rgba(234, 179, 8, 0.6); animation: pulse-warn 1.5s infinite; }
.status-critical #vignette-overlay { box-shadow: inset 0 0 60px rgba(239, 68, 68, 0.8); animation: pulse-crit 0.8s infinite; }

@keyframes pulse-warn {
  0%, 100% { box-shadow: inset 0 0 30px rgba(234, 179, 8, 0.4); }
  50% { box-shadow: inset 0 0 60px rgba(234, 179, 8, 0.8); }
}

@keyframes pulse-crit {
  0%, 100% { box-shadow: inset 0 0 40px rgba(239, 68, 68, 0.5); }
  50% { box-shadow: inset 0 0 80px rgba(239, 68, 68, 0.95); }
}

/* HEADER */
#status-bar {
  background: #1e293b;
  padding: 10px 16px;
  text-align: center;
  border-bottom: 1px solid #334155;
  z-index: 1000;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

#status-text { font-size: 0.85rem; font-weight: 600; color: #f8fafc; }
#timer-display { font-size: 1.2rem; font-weight: bold; color: #38bdf8; }

.action-btn-mini {
  background: #22c55e;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 6px;
  font-weight: bold;
  font-size: 0.8rem;
  cursor: pointer;
}

/* CARTE & ONGLETS */
#main-content { flex: 1; position: relative; }
.tab-page { display: none; width: 100%; height: 100%; }
.tab-page.active { display: block; }
#map { width: 100%; height: 100%; background: #0f172a; }

.map-btn {
  position: absolute;
  bottom: 20px;
  right: 20px;
  z-index: 1000;
  background: rgba(15, 23, 42, 0.9);
  color: white;
  border: 1px solid #334155;
  padding: 10px 16px;
  border-radius: 20px;
  font-weight: bold;
}

/* BULLES AVEC AVATAR SUR LA CARTE */
.custom-map-avatar {
  background: transparent;
  border: none;
}
.avatar-marker-frame {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  border: 3px solid #38bdf8;
  overflow: hidden;
  background: #1e293b;
  box-shadow: 0 4px 10px rgba(0,0,0,0.5);
}
.avatar-marker-frame img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* ONGLET OPTIONS */
#tab-jokers { padding: 16px; overflow-y: auto; background: #0f172a; }
.card { background: #1e293b; border-radius: 12px; padding: 16px; margin-bottom: 16px; border: 1px solid #334155; }
.card h3 { color: #38bdf8; margin-bottom: 8px; font-size: 1.1rem; }
.toggle-label { display: flex; align-items: center; gap: 10px; margin-top: 10px; font-size: 0.9rem; }
#photo-preview img { width: 100%; margin-top: 10px; border-radius: 8px; }

/* FOOTER */
#bottom-bar { background: #1e293b; padding: 10px; border-top: 1px solid #334155; z-index: 1000; }
#nav-tabs { display: flex; gap: 10px; }
.nav-btn { flex: 1; padding: 10px; background: #0f172a; color: #94a3b8; border: 1px solid #334155; border-radius: 8px; font-weight: bold; }
.nav-btn.active { background: #0284c7; color: white; border-color: #0284c7; }