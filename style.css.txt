* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  user-select: none;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

body, html {
  width: 100%;
  height: 100%;
  overflow: hidden;
  background-color: #0f172a;
  color: #f8fafc;
}

/* --- ÉCRAN DU SALON (LOBBY) --- */
.lobby-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  padding: 20px;
  text-align: center;
}

.lobby-container h2 { font-size: 1.8rem; margin-bottom: 4px; color: #38bdf8; }
.subtitle { font-size: 0.9rem; color: #94a3b8; margin-bottom: 24px; }

.lobby-box {
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 16px;
  padding: 20px;
  width: 100%;
  max-width: 360px;
}

.lobby-box h3 { font-size: 1rem; margin-bottom: 12px; color: #e2e8f0; text-align: left; }

.role-select { display: flex; gap: 10px; margin-bottom: 8px; }

.btn-role {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  border: 2px solid #334155;
  background: #0f172a;
  color: #94a3b8;
  font-weight: bold;
}

.btn-role.selected { border-color: #38bdf8; background: #0284c7; color: white; }

.info-text { font-size: 0.8rem; color: #38bdf8; margin-bottom: 16px; text-align: left; }
.divider { border: 0; height: 1px; background: #334155; margin: 16px 0; }

.btn-primary, .btn-secondary, .action-btn {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: none;
  font-weight: bold;
  font-size: 1rem;
}

.btn-primary { background: #0284c7; color: white; margin-bottom: 12px; }
.btn-secondary { background: #334155; color: white; width: auto; padding: 0 16px; }

.join-box { display: flex; gap: 8px; }
.join-box input {
  flex: 1;
  padding: 10px;
  border-radius: 8px;
  border: 1px solid #334155;
  background: #0f172a;
  color: white;
  text-align: center;
  font-size: 1rem;
}

/* --- INTERFACE DE JEU --- */
#app-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100vw;
  transition: box-shadow 0.3s ease, border-color 0.3s ease;
}

/* EFFETS VISUELS CLIGNOTANTS */
.status-normal { border: 4px solid #334155; }
.status-ok { border: 6px solid #22c55e; box-shadow: inset 0 0 20px rgba(34,197,94,0.4); }
.status-warning { border: 6px solid #eab308; animation: pulse-warn 1.5s infinite; }
.status-critical { border: 8px solid #ef4444; animation: pulse-crit 0.8s infinite; }

@keyframes pulse-warn {
  0%, 100% { box-shadow: inset 0 0 10px rgba(234,179,8,0.3); }
  50% { box-shadow: inset 0 0 30px rgba(234,179,8,0.7); }
}

@keyframes pulse-crit {
  0%, 100% { box-shadow: inset 0 0 15px rgba(239,68,68,0.4); }
  50% { box-shadow: inset 0 0 45px rgba(239,68,68,0.9); }
}

/* HEADER & STATUT */
#status-bar {
  background: #1e293b;
  padding: 10px 16px;
  text-align: center;
  border-bottom: 1px solid #334155;
  z-index: 1000;
  position: relative;
}

.room-tag { font-size: 0.75rem; color: #94a3b8; position: absolute; left: 16px; top: 12px; }
.room-tag span { color: #38bdf8; font-weight: bold; }
#status-text { font-size: 0.95rem; font-weight: 600; margin-top: 4px; }
#timer-display { font-size: 1.4rem; font-weight: bold; color: #38bdf8; }

/* CONTENU & CARTE */
#main-content { flex: 1; position: relative; }
.tab-page { display: none; width: 100%; height: 100%; }
.tab-page.active { display: block; }
#map { width: 100%; height: 100%; background: #0f172a; }

.map-btn {
  position: absolute;
  bottom: 20px;
  right: 20px;
  z-index: 1000;
  background: rgba(15, 23, 42, 0.9);
  color: white;
  border: 1px solid #334155;
  padding: 10px 16px;
  border-radius: 20px;
  font-weight: bold;
}

/* ONGLET JOKERS */
#tab-jokers { padding: 16px; overflow-y: auto; background: #0f172a; }
.card { background: #1e293b; border-radius: 12px; padding: 16px; margin-bottom: 16px; border: 1px solid #334155; }
.card h3 { color: #38bdf8; margin-bottom: 8px; font-size: 1.1rem; }
.hint { font-size: 0.8rem; color: #94a3b8; margin-top: 6px; }

.toggle-label { display: flex; align-items: center; gap: 10px; margin-top: 10px; font-size: 0.9rem; }

/* FOOTER */
#bottom-bar { background: #1e293b; padding: 10px; border-top: 1px solid #334155; z-index: 1000; }
#nav-tabs { display: flex; gap: 10px; }
.nav-btn { flex: 1; padding: 10px; background: #0f172a; color: #94a3b8; border: 1px solid #334155; border-radius: 8px; font-weight: bold; }
.nav-btn.active { background: #0284c7; color: white; border-color: #0284c7; }

#photo-preview img { width: 100%; margin-top: 10px; border-radius: 8px; }